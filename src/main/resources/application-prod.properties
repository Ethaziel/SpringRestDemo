# Application name
#spring.application.name=SpringRestDemo

# Server port
#server.port=8080


# H2 console and dev-only configs — remove or disable in production
# spring.h2.console.enabled=true
# spring.h2.console.path=/db-console



# File size limits
#spring.servlet.multipart.max-file-size=10MB
#spring.servlet.multipart.max-request-size=10MB

# PostgreSQL DB config for Render
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

spring.datasource.url=jdbc:postgresql://dpg-d1qo03umcj7s7397ffj0-a.oregon-postgres.render.com:5432/albumdemo_db
spring.datasource.username=petr_slachta
spring.datasource.password=3bJDcy.......   # <== don't commit this in GitHub! Use env variables if possible.
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Schema management (for dev you might use 'create'; for prod use 'update')
spring.jpa.hibernate.ddl-auto=update

# Disable H2 console and settings
spring.h2.console.enabled=false

# Static file handling (optional, for embedded frontend)
spring.mvc.static-path-pattern=/resources/static/**